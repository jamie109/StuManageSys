1. 注册（用户名、密码、身份证、手机号）
用户名唯一，长度在3~15，数字和字母组合、不能是纯数字。
密码输入两次，必须相同。
身份证18位，前17位数字，最后一位可为x、X。
手机号11位，不能以0开头，必须都是数字。
-- 字符串，是否都是数字。
2. 登录
输入用户名、密码，获取验证码输入验证码。
用户名不存在，提示。
验证码不正确，重新输入
用户名和密码是否匹配，三次机会。
3. 忘记密码
用户名不存在，提示未注册。
输入身份证、手机号，如果三者匹配，可以修改。